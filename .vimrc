set nocompatible
syntax enable
filetype plugin on
set path+=**
set wildmenu
" Create the `tags` file (may need to install ctags first)
" command! MakeTags !ctags -R .

set tabstop     =2
set softtabstop =2
set shiftwidth  =2
:set expandtab
